# 测试数据

建议按以下结构登记数据，但默认不提交原始大文件：

```text
fixtures/
├─ manifests/     # 数据清单、授权、标签和校验摘要
├─ generated/     # 可公开的小型合成样本
└─ local/         # 本地真实数据，Git 忽略
```

每份数据清单至少包含：来源、授权范围、采集日期、场景、人数、光照、标签规范、匿名化方式、文件哈希和删除日期。
